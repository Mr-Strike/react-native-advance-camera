import {
  getPlatform,
  imageResizer,
  getFileNameFromURI,
  getFileTypeFromURI,
  convertFileUrlToBase64,
  getImageSource,
  deviceWidth,
  deviceHeight,
} from './helpers'

import {
  LOADER_GIF,
  CROSS_ICON,
  CAMERA_IMAGES,
  GALLERY_IMAGES,
} from './images'

import {
  TOUCHABLE_ACTIVE_OPACITY,
} from './constant'

export {
  getPlatform,
  imageResizer,
  getFileNameFromURI,
  getFileTypeFromURI,
  convertFileUrlToBase64,
  getImageSource,
  deviceWidth,
  deviceHeight,

  LOADER_GIF,
  CROSS_ICON,
  CAMERA_IMAGES,
  GALLERY_IMAGES,

  TOUCHABLE_ACTIVE_OPACITY,
}
